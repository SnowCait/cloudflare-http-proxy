{
  "name": "cloudflare-http-proxy",
  "scripts": {
    "cf-bundle": "wrangler deploy --dry-run --minify --outdir .wrangler/bundle",
    "cf-typegen": "wrangler types",
    "cf-typecheck": "wrangler types --check",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "npm": ">=11.10.0"
  },
  "dependencies": {
    "hono": "^4.12.22"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.4",
    "vitest": "^4.1.8",
    "wrangler": "^4.129.0"
  }
}
